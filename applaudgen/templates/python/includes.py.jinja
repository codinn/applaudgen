{% include 'header.jinja' %}

from .schemas.enums import StringEnum

{% for name, values in include_enums.items() %}
class {{ name|simple_singular|capfirst }}Include(StringEnum):
    {% for value in values %}
    {{ value|snake_case|upper }} = '{{value}}'
    {% endfor %}

{% endfor %}
